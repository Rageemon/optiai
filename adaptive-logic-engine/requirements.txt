# ---------------------------------------------------------------------------
# Neuro-Symbolic Multi-Domain Optimization Engine — Python Dependencies
# ---------------------------------------------------------------------------

# ---- Web Framework --------------------------------------------------------
fastapi>=0.111.0           # ASGI web framework and OpenAPI generation
uvicorn[standard]>=0.29.0  # Production-grade ASGI server (with websockets + watchfiles)

# ---- Data Validation ------------------------------------------------------
pydantic>=2.7.0            # Strict type validation; used for both schemas AND LLM output

# ---- AI / LLM Integration -------------------------------------------------
google-genai>=1.0.0        # Google GenAI SDK (Gemini) — async client + structured output

# ---- Algorithmic Engine ---------------------------------------------------
ortools>=9.9.0             # Google OR-Tools: CP-SAT, VRP, linear solvers

# ---- Map Routing (Multi-Objective OSM-based routing) ----------------------
osmnx>=1.9.0               # OpenStreetMap graph download + NetworkX integration
requests>=2.31.0           # HTTP client for Nominatim geocoding + Overpass API

# ---- Environment & Config -------------------------------------------------
python-dotenv>=1.0.0       # Load .env files into os.environ

# ---- HTTP Client (used internally by google-genai) ------------------------
httpx>=0.27.0
